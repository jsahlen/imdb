begin
  require 'jeweler'
  Jeweler::Tasks.new do |gem|
    gem.name = "imdb"
    gem.summary = "A simple IMDb scraper"
    gem.description = "A simple IMDb scraper"
    gem.email = "johan.sahlen@gmail.com"
    gem.homepage = "http://github.com/jsahlen/imdb"
    gem.authors = ["Johan Sahlén"]
    gem.add_dependency('hpricot')
    gem.add_dependency('json')
  end
rescue LoadError
  puts "Jeweler not available. Install it with: sudo gem install technicalpickles-jeweler -s http://gems.github.com"
end
